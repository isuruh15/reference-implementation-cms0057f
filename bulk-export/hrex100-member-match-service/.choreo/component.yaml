# +required The configuration file schema version
schemaVersion: 1.1

# +optional Incoming connection details for the component
endpoints:
  # +required Unique name for the endpoint.
  # This name will be used when generating the managed API
  - name: member-match-service
    # +optional Display name for the endpoint.
    displayName: Member Match Service
    # +required Service section has the user service endpoint details
    service:
      # +optional Context (base path) of the API that gets exposed via the endpoint.
      basePath: /
      # +required Numeric port value that gets exposed via the endpoint
      port: 8091
    # +required Type of traffic that the endpoint is accepting.
    # Allowed values: REST, GraphQL, WS, GRPC, TCP, UDP.
    type: REST
    # +optional Network level visibilities of the endpoint.
    # Accepted values: Project|Organization|Public(Default).
    networkVisibilities: 
      - Public
      - Organization
    # +optional Path to the schema definition file. Defaults to wild card route if not provided
    # This is only applicable to REST or WS endpoint types.
    # The path should be relative to the docker context.
    schemaFilePath: openapi/match_openapi.yaml
dependencies:
    connectionReferences:
    - name: old-payer-patient-connection
      resourceRef: service:/unitedcare-standard-plan/patient-service/v1/803f0/PROJECT
    - name: old-payer-coverage-connection
      resourceRef: service:/unitedcare-standard-plan/coverage-service/v1/6c05f/PROJECT
# dependencies:
#     connectionReferences:
#     - name: patient-cms-connection
#       resourceRef: service:/cms-0057-f/patient-service/v1/803f0/PROJECT
#     - name: coverage-cms-connection
#       resourceRef: service:/cms-0057-f/coverage-service/v1/803f0/PROJECT